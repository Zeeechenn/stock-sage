# 已修复问题

> 任务编号体系：M0–M6 里程碑 + M{X}.{Y} 子任务（见 `PROJECT.md` 顶部术语映射表）。本文档历史标题已迁移到 M 编号。

## 2026-05-15 — M1.4（旧"执行计划 A 修复轨"）
- RSI 在单边上涨/下跌/横盘时不再产生持续 NaN。
- 聚合分数遇到 NaN/Inf 时回退为中性 0。
- 组合仓位计算保留现金，不再为了凑满 100% 破坏单股上限。
- 长期标签缺失时强买降级为买入。
- `/signals/eval` 将 long/neutral/short 分开统计，观望不再进入卖出收益。
- `save_signal()` 改为同日 upsert，并写入 `rule_version` 与 `data_timestamp`。
- Tavily 搜索拆分为通用 query 与个股新闻包装函数。

## 2026-05-15 — M1.6（旧"执行计划 C 规则轨"）
- 信号语言从"强买/买入/卖出/强卖"调整为"可小仓试错/可关注/观望/规避"。
- 单股上限调整为 15%，新增单板块 30%、总权益仓位 80%、新信号试错仓 5%。
- RSI 超买不再直接给卖出分，仅作为风险提示基础。

## 2026-05-15 — M1.3（长期分析师团 first batch）
- 顺手修 `decision/memory_layered.py:174` 的 `complete_text()`。

## 2026-05-15 — M3 二阶审计层
- 通过 DSR 回算发现：M1.1 当时"IC=0.0228 < 0.03 → Qlib 不合格"的判定方法忽略样本量。N=12797 下 t=2.58、p=0.0099，IC 实际**极显著**。最终"Qlib 归零"决策保留（分层非单调是独立证据），但记录这条教训：单 IC 阈值不应裸用。
