# Paper Trading Review

Read:

```bash
python3 -m backend.agent.cli project-context --pretty
```

Then summarize:

- current test/profile context;
- win rate, drawdown and net PnL signals if available;
- exit reasons and risk-rule behavior;
- concrete next checks before changing thresholds or weights.
